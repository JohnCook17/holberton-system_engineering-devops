#!/usr/bin/env bash
#clock
hour=0
minute=60
while ((hour < 12))
do
    if [ $minute = 60 ]
    then
        minute=1
        echo "Hour: "$hour
        hour=$((hour+1))
    fi
    echo $minute
    minute=$((minute+1))
done